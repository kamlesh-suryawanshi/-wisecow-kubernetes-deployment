name: Build Push and Deploy Wisecow

on:
  push:
    branches:
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build -t ghcr.io/kamlesh-suryawanshi/wisecow:latest .

      - name: Push Docker Image
        run: |
          docker push ghcr.io/kamlesh-suryawanshi/wisecow:latest

  deploy:
    needs: docker
    runs-on: self-hosted

    steps:
      - name: Restart Kubernetes Deployment
        run: |
          kubectl rollout restart deployment wisecow

      - name: Check Rollout Status
        run: |
          kubectl rollout status deployment wisecow